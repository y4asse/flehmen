package main_test

import (
	"testing"

	"github.com/labstack/echo/v4"
)

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestController_GetBetween(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		c       echo.Context
		wantErr bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var controller main.Controller
			gotErr := controller.GetBetween(tt.c)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBetween() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBetween() succeeded unexpectedly")
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestGetClosenessScore(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		chatText string
		want     *main.ClosenessScore
		wantErr  bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, gotErr := main.GetClosenessScore(tt.chatText)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetClosenessScore() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetClosenessScore() succeeded unexpectedly")
			}
			// TODO: update the condition below to compare got with tt.want.
			if true {
				t.Errorf("GetClosenessScore() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestController_GetBetween(t *testing.T) {
	tests := []struct {
		name string // description of this test case
		// Named input parameters for target function.
		c       echo.Context
		wantErr bool
	}{
		// TODO: Add test cases.
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// TODO: construct the receiver type.
			var controller main.Controller
			gotErr := controller.GetBetween(tt.c)
			if gotErr != nil {
				if !tt.wantErr {
					t.Errorf("GetBetween() failed: %v", gotErr)
				}
				return
			}
			if tt.wantErr {
				t.Fatal("GetBetween() succeeded unexpectedly")
			}
		})
	}
}
